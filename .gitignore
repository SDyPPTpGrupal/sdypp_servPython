__pycache__/
*.py[cod]
*$py.class
.env

# Entornos virtuales: nunca se versionan, llevan binarios y rutas absolutas
# de la máquina que los creó.
.venv/
venv/
env/

# Stubs generados desde contrato.proto: son producto del build, no fuente.
# Se regeneran con el comando del README o con el Dockerfile.
app/contrato_pb2.py
app/contrato_pb2.pyi
app/contrato_pb2_grpc.py

# Bitácora y logs: son del disco local de cada nodo (CONTRATO.md §9).
logs/
deploy/estado/
